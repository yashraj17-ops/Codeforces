#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    int ans = max({
        a + b + c,
        (a + b) * c,
        (a + c) * b,
        (b + c) * a,
        a * b * c
    });

    cout << ans << endl;
}
